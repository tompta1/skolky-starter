# Copy this to .env.local for local development.
# Leave empty (or omit the file entirely) when deploying to Vercel —
# the frontend and API share the same origin there, so relative URLs work.
VITE_API_BASE=http://localhost:8000
